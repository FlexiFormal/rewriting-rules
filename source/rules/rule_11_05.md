Rule 11.5: "And"-Idempotence Reduction
======================================


```{rewrite-rule}
$\vdash \forall \varphi.\, \varphi \wedge \varphi = \varphi$

{ph}`PP` and {ph}`PP`

{ph}`PP`
```


Examples
--------

```{rewrite-rule}
$x$ is prime and $x$ is prime

$x$ is prime
```
