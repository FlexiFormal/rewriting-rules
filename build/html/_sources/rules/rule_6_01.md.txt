Rule 6.1: $\iota_\in$-Reduction
===============================


```{rewrite-rule}
{ph}`a` : {cat}`Term` = {nl}`$0$`

{ph}`b` : {cat}`Term` = {nl}`$b$`

${ph}`b` \in \{{ph}`a`\}$

${ph}`b` = {ph}`a`$
```


Examples
--------

```{rewrite-rule}
$b \in \{0\}$

$b = 0$
```
